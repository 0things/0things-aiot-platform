# 0things AIoT 平台 TODO

> 目标：把当前“设备、产品、OTA、遥测、规则”管理能力推进到可稳定运营的 IoT 平台。
>
> 优先级：P0 = 上线前必须补齐；P1 = 核心运营能力；P2 = 体验与扩展能力。

## P0：平台基础与生产安全

- [ ] **组织与权限闭环**
  - 完成组织、成员、邀请、移除、角色和资源权限；
  - 所有产品、设备、OTA、规则、遥测查询和写入都校验组织边界；
  - 去除 `internal/tenant` 中用于未接入真实认证时的默认组织兜底。
- [ ] **认证与会话安全**
  - 统一登录、Token 刷新、退出、过期和并发会话策略；
  - 补齐管理员、运维、只读、设备开发等最小权限角色；
  - 增加登录失败限制、敏感操作二次确认和审计记录。
- [ ] **审计与合规**
  - 记录设备命令、OTA 发布、规则修改、成员权限变更和数据导出；
  - 支持按操作者、组织、资源、时间和结果检索；
  - 明确审计数据保留期、脱敏和导出策略。
- [ ] **可靠性基线**
  - 完成 PostgreSQL、Redis、Kafka 的健康检查、超时、重试和降级；
  - 建立数据库备份、恢复演练、迁移回滚和数据保留策略；
  - 为后端、遥测服务和异步任务补齐结构化日志、指标、Trace ID 与告警。
- [ ] **配置与密钥管理**
  - 生产密钥、MQTT 凭据、数据库 DSN 与签名密钥接入 Secret 管理；
  - 启动时校验必填配置，禁止使用隐式默认凭据或默认组织；
  - 区分开发、测试、预发布和生产配置。

## P0：设备生命周期

- [ ] **设备注册与安全入网**
  - 支持批量导入、设备凭据初始化、证书/密钥轮换和吊销；
  - 明确设备唯一标识、设备 Key 冲突、重复注册和转移规则；
  - 支持设备激活、禁用、注销、恢复及对应审计。
- [ ] **设备在线状态闭环**
  - 以遥测/连接事件驱动在线、离线和最后在线时间；
  - 统一在线状态判定、心跳超时和网络抖动处理；
  - 前端设备详情与总览展示真实状态，不使用静态或推测数据。
- [ ] **设备命令可靠投递**
  - 实现命令 ID、幂等键、发送确认、设备 ACK、超时、重试和失败原因；
  - 保存完整命令生命周期，支持取消、重放和权限校验；
  - 将当前测试用的 `mock-kafka` 能力与生产命令链路明确隔离。

## P0：遥测、事件与数据闭环

- [ ] **遥测数据持久化**
  - `telemetry-service/internal/data/telemetry.go` 当前仍以占位逻辑为主，需要落库或接入时序存储；
  - 明确指标、属性、单位、采样时间、设备时间与服务接收时间；
  - 支持按设备、产品、指标和时间范围查询，并设置冷热数据保留策略。
- [ ] **事件处理闭环**
  - 事件接收、校验、去重、持久化、消费失败重试和死信处理；
  - 形成设备事件时间线，支持按事件类型、严重级别和时间检索；
  - 补齐 Kafka 消费积压、丢失和重复消费监控。
- [ ] **设备连接状态实际更新**
  - `telemetry-service/internal/data/client_connect_state.go` 中的连接状态写入和设备状态更新仍需实现；
  - 处理重复连接、断线、重连、跨实例和乱序事件。

## P1：OTA 升级生产能力

- [ ] **升级任务与批次状态机**
  - 明确批次创建、排队、下发、设备确认、成功、失败、取消和超时状态转换；
  - 每个设备在每个批次保留独立记录，并增加尝试次数、错误、开始/结束时间和目标版本；
  - 设备上报状态必须关联准确的批次，避免只用“设备 + OTA 包”定位记录。
- [ ] **真实传输边界**
  - 引入独立 OTA Dispatcher，区分“数据库已排队”和“升级命令已送达”；
  - 支持 MQTT/HTTP 等传输适配、并发限制、退避重试、超时和死信；
  - 只有设备确认目标版本后才判定升级成功。
- [ ] **升级安全与发布策略**
  - 固件 SHA-256 校验、签名校验、密钥轮换和安全下载地址；
  - 支持灰度、分组、分批、暂停、继续、取消、失败重试和回滚；
  - 校验目标产品、硬件型号、当前版本和兼容范围。
- [ ] **OTA 历史与运营报表**
  - 展示批次、设备尝试、失败原因、耗时、成功率和版本分布；
  - 支持按批次/设备/状态/时间搜索、导出和审计追踪；
  - 为大批量任务增加分页、异步导出和查询索引。

## P1：产品与物模型（TSL）

- [ ] **TSL 生命周期**
  - 属性、服务、事件的数据类型、单位、范围、必填和枚举校验；
  - 草稿、校验、发布、版本、回滚和兼容性检查；
  - 防止已绑定设备因物模型变更产生不可解释的数据。
- [ ] **消息解析与协议管理**
  - Topic、上行/下行消息、协议版本和解析器的可视化配置；
  - 解析失败样本、重试、死信和回放工具；
  - 解析器变更的灰度发布与回滚。

## P1：规则、告警与通知

- [ ] **规则引擎执行闭环**
  - 规则触发、条件计算、动作执行、幂等、顺序和失败重试；
  - 支持设备、产品、分组和组织级作用域；
  - 保存每次触发的输入、命中条件、动作结果和耗时。
- [ ] **告警中心生产化**
  - 告警规则、去重、聚合、抑制、升级、确认、关闭和恢复；
  - 邮件、Webhook、短信或企业 IM 通知渠道；
  - 通知模板、发送记录、失败重试和租户级开关。

## P1：运营分析与开放能力

- [ ] **真实运营总览**
  - 设备总量、在线率、消息量、告警、升级和规则指标全部来自统一数据源；
  - 明确统计口径、时区、延迟和数据刷新时间；
  - 支持按组织、产品、设备分组和时间范围下钻。
- [ ] **报表与导出**
  - 设备、遥测、事件、告警和 OTA 报表支持异步生成、下载权限和审计；
  - 大数据量导出不阻塞 API 请求，并提供进度与失败重试。
- [ ] **开放 API 与 Webhook**
  - 统一 API 版本、错误码、分页、幂等、限流和签名校验；
  - 提供 API Key/应用管理、权限范围、调用日志和配额；
  - Webhook 支持签名、重试、顺序、去重和回调状态查询。

## P2：前端体验与运维效率

- [ ] 统一所有列表页的搜索提交、筛选、分页、空状态、错误重试和加载状态行为。
- [ ] 完善设备详情：实时数据、命令历史、事件时间线、影子、标签和 OTA 历史联动。
- [ ] 增加批量操作的预览、影响范围、进度、部分成功结果和失败项下载。
- [ ] 完善中英文国际化，清理硬编码文案、占位数据和重复 namespace key。
- [ ] 增加窄屏布局、键盘操作、焦点管理、表格可读性和基础无障碍检查。
- [ ] 提供系统设置：时区、数据保留、通知策略、默认分页和组织级功能开关。

## P0：测试与交付门禁

- [ ] 为租户隔离、权限拒绝、批次重复提交、设备状态乱序和消息重复消费增加测试。
- [ ] 为 OTA 状态机、重试、回滚、批次历史和迁移兼容性增加集成测试。
- [ ] 建立前端关键流程的浏览器验收：登录、设备注册、命令下发、OTA、规则和告警。
- [ ] 建立后端/遥测服务的契约测试，确保 OpenAPI 与 protobuf 生成代码同步。
- [ ] CI 固化 `pnpm build`、`pnpm lint`、`make test`、遥测服务构建、迁移检查和依赖安全扫描。
- [ ] 发布前验证数据库迁移、回滚、备份恢复、远程配置和真实依赖连通性。

## 建议实施顺序

1. 先完成组织权限、审计、配置密钥和可观测性，确保后续能力可安全运营。
2. 再打通设备入网、连接状态、命令可靠投递和遥测持久化，形成设备数据闭环。
3. 然后完善 OTA Dispatcher、批次状态机、签名校验、灰度和回滚。
4. 最后扩展规则告警、运营分析、开放 API 和前端体验。

## 当前已具备的基础能力

- 产品、设备、设备分组、TSL、OTA、规则场景、用户和基础设置页面。
- Gin REST API、Swagger、Kratos HTTP/gRPC 遥测服务和 Kafka 接入骨架。
- OTA 包、批次和设备部署记录的基本管理能力。
- 前端中英文资源、列表分页、部分设备详情与操作审计基础。

