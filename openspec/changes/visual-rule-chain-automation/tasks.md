## 1. 节点目录与领域模型

- [x] 1.1 新增 `rule_node_definitions` GORM 模型并接入迁移，验证 SQLite 创建 `key` 与 `uuid` 唯一索引及 JSON 文本字段
- [x] 1.2 为系统入口、七类默认节点及 ThingsBoard 对应节点建立幂等 Seeder，验证首次写入完整目录、再次运行不重复且不覆盖已存在定义
- [ ] 1.3 新增规则链、范围、版本、节点、边和审计日志 GORM 模型及迁移，并验证组织范围字段与已发布定义引用保护
- [ ] 1.4 新增统一规则 Outbox、执行、节点事件、节点状态和计划任务模型及迁移，并验证重复 `(version_id, message_id)` 不会建立两条执行记录且状态按节点/实体隔离
- [ ] 1.5 新增告警、告警迁移，并为规则节点增加密文配置列及调用尝试字段，验证活动告警、凭据脱敏和节点级调用去重约束
- [ ] 1.6 通过项目流程生成 DAL 和 Wire 依赖，并验证 `make -C backend gen` 成功且生成产物未手改

## 2. 规则图管理与 API

- [ ] 2.1 定义 `RuleMessage`、Go 执行器白名单和关系类型，并验证每个节点目录项的配置 schema、端口、版本与执行器一致
- [ ] 2.2 实现草稿创建、读取、更新、复制和软删除的 Repository/Service/Handler，并验证组织隔离和活跃名称唯一性测试
- [ ] 2.3 实现图校验，覆盖唯一输入、可达性、端口兼容、DAG、配置、范围和资源引用，并验证循环与孤立动作被拒绝
- [ ] 2.4 实现发布、停用、版本列表和历史恢复的不可变快照流程，并验证历史执行关联原发布版本
- [ ] 2.5 新增规则链、版本、测试、节点定义和导入导出 DTO/Swagger 路由，并验证 `make -C backend swag` 成功

## 3. 消息执行与调试

- [ ] 3.1 在遥测、属性、RPC、设备状态、告警、实体、分组、评论和受控 REST 持久化事务中写入统一规则 Outbox，并验证接入回调不等待规则执行且自定义消息类型无需迁移
- [ ] 3.2 实现 Outbox 消费与适用发布链分发，并验证产品、设备组和组织范围只接收匹配消息
- [ ] 3.3 实现关系驱动执行器和节点事件记录，并验证 `True`/`False`/`Success`/`Failure` 分支和扇出回放
- [ ] 3.4 实现七类目录中的默认节点：筛选、增强、转换、动作、Webhook、流程和分析，并验证成功、配置失败和运行失败路径
- [ ] 3.5 实现计划任务原子领取、恢复和节点幂等，并验证 Worker 重启后延迟任务不丢失且不重复副作用
- [ ] 3.6 实现子规则链调用和跨链循环校验，并验证父链接收子链终止关系且递归被拒绝
- [ ] 3.7 实现模拟测试运行与节点事件查询，并验证测试不出站、不下发命令且可查看完整模拟结果

## 4. 告警与 Webhook

- [ ] 4.1 实现告警创建、聚合、确认、恢复和关闭及 API，并验证活动去重、状态迁移和执行关联
- [ ] 4.2 实现 Webhook 节点公开配置与密文配置的保存、读取脱敏和导出清除，并验证 API、日志和图快照不返回凭据
- [ ] 4.3 定义平台 `WebhookSender`，实现受限请求体映射、签名和幂等键，并验证节点凭据不泄漏到规则图 DTO
- [ ] 4.4 实现 Webhook HTTPS/SSRF/重定向限制、超时、退避与节点事件记录，并验证私网目标拒绝、失败进入 `Failure`
- [ ] 4.5 实现节点内 Webhook 测试、调用去重和执行详情回放，并验证一次逻辑调用的全部尝试均可追溯

## 5. 前端基础与画布

- [ ] 5.1 根据 Swagger 运行 `pnpm -C frontend generate:api`，并验证 `src/api/generated/` 只由生成命令更新
- [ ] 5.2 添加 `@xyflow/react` 与自动布局依赖，建立 `rule-automation` 功能域和路由，并验证前端构建成功
- [ ] 5.3 实现规则链、节点目录、告警、运行记录列表页并验证筛选、分页、空态和权限行为
- [ ] 5.4 维护全部新增中英文 i18n，并验证切换语言没有缺失 key
- [ ] 5.5 实现分类节点面板与自定义节点、具名 Handle、关系边，并验证可拖入首期节点且不兼容端口不能连接
- [ ] 5.6 实现画布平移缩放、MiniMap、框选、复制粘贴、删除、撤销重做、Fit View 和自动布局，并验证键鼠完整编辑流程
- [ ] 5.7 实现动态 shadcn 配置抽屉和 Webhook 配置表单，并验证属性类型限制运算符、凭据脱敏和校验错误同时显示于节点和表单
- [ ] 5.8 实现保存、未保存离开确认、校验定位、测试、发布和运行覆盖层，并验证模拟与真实运行路径均可回放

## 6. 集成验证

- [ ] 6.1 编写端到端测试：属性变化 → 阈值 → 创建/恢复告警 → 模拟与真实 Webhook，并验证告警、执行、事件和投递完整关联
- [ ] 6.2 编写跨组织、凭据脱敏、Webhook SSRF、重复消息、Worker 重启和版本回滚测试，并验证符合规格
- [ ] 6.3 执行 `make test && make build`，修复本变更引入失败并记录结果
- [ ] 6.4 执行 `pnpm -C frontend format && pnpm -C frontend build`，修复本变更引入失败并记录结果
