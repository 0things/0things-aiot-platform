## 1. 后端调用记录与查询接口

- [x] 1.1 新增 `device_service_invocations` GORM 模型，包含 UUID、设备关联、服务标识符与名称快照、JSON 入/出参及调用时间，并注册迁移与 GORM DAL 生成；验证生成代码不被手改且表索引满足设计文档。
- [x] 1.2 实现调用记录 Repository 与 Service 的设备归属校验、服务标识符精确筛选、闭区间时间筛选、按调用时间倒序的服务端分页；验证仓储和服务测试覆盖默认分页、组合筛选、空输出和不存在设备。
- [x] 1.3 实现 `GET /devices/:deviceKey/thing-model-service-invocations` 的 v1 DTO、Handler、Router 与 Swagger 注释；验证处理器测试覆盖默认参数、非法参数、未找到设备和响应分页元数据。
- [x] 1.4 生成 Swagger 与前端 API 客户端；验证生成接口包含路径参数、筛选参数以及调用记录响应类型，且生成产物无手工编辑。

## 2. 设备详情服务调用列表

- [x] 2.1 为服务调用记录新增前端查询封装和缓存键，使用生成的 Hook 请求当前设备的服务端分页接口；验证请求携带设备标识、页码、页大小和已应用筛选条件。
- [x] 2.2 以调用记录表格替换设备详情「物模型数据 → 服务调用」占位内容，固定渲染时间、标识符、服务名称、输入参数和输出参数五列；验证首次加载仅查询最近 7 天且默认每页 20 条。
- [x] 2.3 实现服务标识符和秒级起止时间的草稿筛选、查询、刷新与重置交互；验证提交或重置筛选都会回到第 1 页且保留正确的时间范围。
- [x] 2.4 复用 `DataTablePagination` 接入 10/20/30/40/50 页大小与服务端翻页，并为参数单元格实现截断预览和完整 JSON 详情弹窗；验证页大小变化重置首页、无输出显示 `-`、详情可显示格式化 JSON。
- [x] 2.5 补齐中英文服务调用列表文案以及加载、错误、重试和空数据状态；验证两份 locale JSON 均可解析且页面不含硬编码新增文案。

## 3. 整体验证

- [x] 3.1 增加后端集成测试，验证持久化的服务调用记录可按设备、标识符和时间范围分页读取；运行 `make test`。
- [x] 3.2 增加前端组件或集成测试，覆盖首屏、筛选、分页、JSON 详情、加载/错误/空态；运行 `pnpm -C frontend format && pnpm -C frontend build`。
