## 1. 数据模型与迁移

- [x] 1.1 新增 `DeviceGroup` 模型和 `device_groups` 表，使用 Go UUID v4 生成 `group_uuid`，包含组织、类型、规则、`deleted_at` 及唯一约束，并验证迁移可重复执行
- [x] 1.2 新增 `DeviceGroupMember` 模型和成员唯一约束，验证同一设备不能在同一手动分组中重复
- [x] 1.3 注册模型迁移并生成 GORM DAL，验证 `make gen` 成功且生成查询包含分组模型

## 2. 后端分组领域能力

- [x] 2.1 实现分组 Repository，所有查询带组织条件并通过 UUID 定位，验证跨组织数据不可见
- [x] 2.2 实现动态规则解析与白名单校验，支持设备字段、标签字段及 `=`、`!=`、`LIKE`、`IN`、`AND`、`OR`，验证非法规则不会执行查询
- [x] 2.3 实现分组 Service，完成 UUID 生成、组织内活跃名称校验、手动/动态类型约束和软删除处理，验证对应单元测试
- [x] 2.4 实现分组 CRUD、成员管理和两个动态规则预览 Handler，公开接口只接收和返回 `groupUuid`，验证 Swagger 注释完整
- [ ] 2.5 为设备列表增加可选 `groupUuid` 筛选，并增加设备所属分组查询，验证分页、组织权限和多手动分组结果
- [x] 2.6 注册全部分组和设备分组路由，确保静态 `/device-groups/preview` 在通配路由之前注册，并增加路由回归测试验证 UUID 路由不冲突

## 3. 后端验证与接口产物

- [ ] 3.1 增加分组领域、规则解析、Handler 和路由测试，覆盖手动成员、动态预览、跨组织和不存在 UUID
- [x] 3.2 运行 `cd backend && make swag`，验证 Swagger 包含全部分组接口且无旧 ID 参数
- [ ] 3.3 运行 `cd backend && go test ./...`，验证后端完整测试通过

## 4. 前端分组管理

- [x] 4.1 根据 Swagger 生成分组 API 客户端和类型，验证 `pnpm generate:api` 成功且调用参数使用 `groupUuid`
- [ ] 4.2 新增设备分组列表和分组详情页面，验证加载、分页、编辑和删除
- [ ] 4.3 新增创建/编辑分组对话框，支持“默认（手动）/动态”类型、名称和描述，验证中英文文案齐全
- [ ] 4.4 新增动态规则编辑与预览交互，展示字段/操作符提示、搜索、重置和校验错误，验证预览不会保存规则
- [ ] 4.5 在设备列表增加分组筛选，在设备详情接入分组 Tab，分别展示手动所属和动态命中的分组
- [ ] 4.6 统一 React Query 缓存键使用 `groupUuid`，运行 `pnpm exec tsc -b`、`pnpm lint` 和 `pnpm format:check`

## 5. 集成验收

- [ ] 5.1 使用真实组织和设备数据验证创建手动/动态分组、设备筛选、设备详情展示及跨组织隔离
- [ ] 5.2 验证分组软删除不删除设备且已删除分组不可见，动态规则修改后成员结果实时变化，并记录验收结果
