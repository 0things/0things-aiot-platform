# Copy to a private file outside the repository; never commit real credentials.
CONFIG_APP_WIFI_SSID="your-network"
CONFIG_APP_WIFI_PASSWORD="replace-me"
CONFIG_APP_BROKER="mqtt.example.com"
CONFIG_APP_MQTT_USERNAME="device-user"
CONFIG_APP_MQTT_PASSWORD="replace-me"
